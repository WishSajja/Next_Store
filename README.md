This an  ecommerce platform with authectication functionality using Identity. It also uses EntityFramework for the Object Relational
mapping together with SQL Server as the primary database. All this using **Asp.Net MVC**
